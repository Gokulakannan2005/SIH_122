{
  "name": "datum-platform",
  "private": true,
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node scripts/start-dev.mjs",
    "build": "cd backend && npm install",
    "start": "node --experimental-strip-types --experimental-sqlite backend/src/server.ts",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev"
  }
}

